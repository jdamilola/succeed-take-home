# Server Configuration
PORT=9999
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your_jwt_secret
JWT_EXPIRES_IN=24h

# Database Connection (if not using Supabase directly)
DATABASE_URL=postgres://username:password@localhost:5432/database 